/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;

export const metadata = {
    title: "The Writer",
    description: "The blogger of the future",
  };
  
  // The rest of your layout component can remain the same as server-side code
  